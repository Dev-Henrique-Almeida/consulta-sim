<!DOCTYPE html>
<html>
<head>
  <title>Dados da consulta</title>
  <style>
      body {
          font-family: Arial, sans-serif;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          height: 70vh;
          margin: 0;
          padding: 0;
      }

      h1 {
          text-align: center;
      }

      .btn {
          padding: 10px 20px;
          font-size: 16px;
          color: white;
          border: none;
          cursor: pointer;
      }

      .btn:hover {
          opacity: 0.8;
      }

      .btn-primary {
          background-color: #4CAF50;
      }

      .btn-primary:hover {
          background-color: #45a049;
      }

      .btn-secondary {
          background-color: gray;
      }

      .btn-remove {
          background-color: #ff6666;
      }

      .btn-remove:hover {
          background-color: #ff9999;
      }
  </style>
</head>
<body>
<p style="color: green"><%= notice %></p>

<h1>Dados da consulta</h1>

<%= render @consultum %>

<div>
  <%= button_to "Editar esta consulta", edit_consultum_path(@consultum), method: :get, class: "btn btn-primary" %>
  <br>
  <%= button_to "Remover esta consulta", @consultum, method: :delete, class: "btn btn-remove" %>
  <br>
  <%= button_to "Voltar", consulta_path, method: :get, class: "btn btn-secondary" %>
</div>

</body>
</html>
