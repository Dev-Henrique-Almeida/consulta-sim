<!DOCTYPE html>
<html>
<head>
  <title>Pacientes</title>
  <style>
      body {
          font-family: Arial, sans-serif;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          height: 70vh;
          margin: 0;
          padding: 0;
      }

      h1 {
          text-align: center;
      }

      #pacientes {
          display: flex;
          flex-direction: column;
          align-items: center;
          margin-top: 20px;
          overflow-y: auto;
          max-height: 400px;
      }

      .paciente {
          margin-bottom: 10px;
      }

      .paciente p {
          margin-bottom: 5px;
      }

      .btn {
          padding: 10px 20px;
          font-size: 16px;
          color: white;
          border: none;
          cursor: pointer;
      }

      .btn:hover {
          opacity: 0.8;
      }

      .btn-primary {
          background-color: #4CAF50;
      }

      .btn-primary:hover {
          background-color: #45a049;
      }

      .btn-group {
          margin-top: 20px;
          display: flex;
      }

      .btn-group button {
          margin-right: 10px;
      }

      .btn-gray {
          background-color: gray;
      }
  </style>
</head>
<body>
<p style="color: green"><%= notice %></p>

<h1>Pacientes</h1>

<div id="pacientes">
  <% @pacientes.each do |paciente| %>
    <div class="paciente">
      <%= render paciente %>
      <br>
      <%= button_to "Mostrar dados do paciente", paciente, method: :get, class: "btn btn-primary" %>
    </div>
  <% end %>
</div>

<div class="btn-group">
  <form action="/pacientes/new" method="get">
    <button type="submit" class="btn btn-primary">Cadastrar um novo paciente</button>
  </form>
  <form action="/" method="get">
    <button type="submit" class="btn btn-gray">Voltar</button>
  </form>
</div>
</body>
</html>
